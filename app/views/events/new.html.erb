<%= form_for @event, :url => { :controller => "events", :action => "create" } do |f|  %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :description %>
    <%= f.text_area :description %>
    <%= f.submit %>
<% end %>