<%= @event.name %>
<%= form_for @location , :url => event_locations_path(@event), :method => :patch do |f| %>
    <%= render :partial => 'form', :locals => {:f => f , :location => @location} %>
<% end %>